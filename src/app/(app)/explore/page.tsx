// The previous Explore tab (feed/배너/검색/컬렉션/크리에이터) was removed —
// a new feature is going in here. Keeping the route (and its bottom-nav
// tab) alive as an empty placeholder rather than deleting it outright.
export default function ExplorePage() {
  return null;
}
